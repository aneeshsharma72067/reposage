{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "npm run prisma:generate",
    "functions": {
        "api/index.ts": {
            "maxDuration": 30,
            "includeFiles": "prisma/**"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index.ts"
        }
    ]
}